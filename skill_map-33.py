# === Stage 33: Добавь откат последнего действия там, где это разумно ===
# Project: SkillMap
def undo():
    """Откат последнего действия: возвращает пользователем выбранное действие в исходное состояние.
    
    Если в истории нет действий — выводит сообщение и ничего не делает.
    """
    if not history:
        print("История пуста, отменить нечего.")
        return
    
    action = history.pop()
    print(f"Отменено действие: {action.get('name', 'Безымянное')}")
